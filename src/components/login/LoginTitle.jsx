import Typography from "@mui/joy/Typography";

const LoginTitle = () => {
    return (
        <Typography level="h1" sx={{fontWeight: 'bold'}}>
            <b>Log in to Soundtify</b>
        </Typography>
    );
};

export default LoginTitle;